const CustomAPIError = require("./custom-error");
const UnautenticatedError = require("./unautenticated");
const BadRequest = require("./bad-request");

module.exports = {
  CustomAPIError,
  UnautenticatedError,
  BadRequest,
};
